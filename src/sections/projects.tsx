import React from 'react';
import { motion } from 'framer-motion';

export default function Projects() {
  return (
    <motion.div initial={{ backgroundColor: 'black' }} exit={{ backgroundColor: 'black' }} animate={{ backgroundColor: 'black' }}>
      Projects
    </motion.div>
  );
}
